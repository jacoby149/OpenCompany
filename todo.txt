get acct info + gh token in an encoded JWT [don't just pass the raw gh token]
create the promotion structure in a python file
make the promotion UI
allow referrals to 
per user, save the : 
    promotion rank.
    referees per rank.
allow users to enter billing info for affiliate capability